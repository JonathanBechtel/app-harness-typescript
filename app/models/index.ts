/**
 * The canonical schema: every table module is re-exported here so drizzle sees
 * the whole schema (`drizzle(client, { schema })`) and drizzle-kit can diff it.
 * Add `export * from './<aggregate>.js';` when you add a table module.
 */

export {};
